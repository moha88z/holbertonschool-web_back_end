#!/usr/bin/env python3
"""Module that contains a function to_str which converts
a float to its string representation.
"""


def to_str(n: float) -> str:
    """Return the string representation of a float."""
    return str(n)
